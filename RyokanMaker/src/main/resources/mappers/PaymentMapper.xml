<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mnu.ryokanmaker.mapper.PaymentMapper">

    <!--
        RESV_NUM / ROOM_RESV_NUM은 IDENTITY 컬럼(GENERATED BY DEFAULT AS IDENTITY)이라
        INSERT 시 컬럼을 생략하면 Oracle이 자동 채번한다.
        useGeneratedKeys로 채번된 값을 다시 dto(resvNum / roomResvNum)에 담아온다.
    -->
    <insert id="insertReservation" parameterType="com.mnu.ryokanmaker.dto.ReservationDto"
            useGeneratedKeys="true" keyProperty="resvNum" keyColumn="RESV_NUM">
        INSERT INTO RESERVATION (
            AdminIdx, UserMail, Resv_Price, Resv_People,
            Resv_Status, Resv_PayStatus, Resv_PayMethod,
            Resv_ArrivalTime, Resv_Request, Resv_Day, Resv_Order_Id
        ) VALUES (
            #{adminIdx}, #{userMail}, #{resvPrice}, #{resvPeople},
            #{resvStatus}, #{resvPayStatus}, #{resvPayMethod},
            #{resvArrivalTime}, #{resvRequest}, #{resvDay}, #{resvOrderId}
        )
    </insert>

    <insert id="insertRoomReservation" parameterType="com.mnu.ryokanmaker.dto.RoomReservationDto"
            useGeneratedKeys="true" keyProperty="roomResvNum" keyColumn="ROOM_RESV_NUM">
        INSERT INTO ROOM_RESERVATION (
            UserMail, AdminIdx, RoomIdx, Resv_num, PlanIdx,
            Resv_CheckIn, Resv_CheckOut, Resv_Price, Resv_People,
            Resv_Status, Resv_PayStatus, Resv_PayMethod
        ) VALUES (
            #{userMail}, #{adminIdx}, #{roomIdx}, #{resvNum}, #{planIdx},
            #{resvCheckIn}, #{resvCheckOut}, #{resvPrice}, #{resvPeople},
            #{resvStatus}, #{resvPayStatus}, #{resvPayMethod}
        )
    </insert>

    <update id="updatePayStatusByOrderId">
        UPDATE RESERVATION
        SET Resv_PayStatus = #{resvPayStatus},
            Resv_PayMethod = #{resvPayMethod}
        WHERE Resv_Order_Id = #{resvOrderId}
    </update>

    <select id="selectResvPriceByOrderId" resultType="Integer">
        SELECT Resv_Price
        FROM RESERVATION
        WHERE Resv_Order_Id = #{resvOrderId}
    </select>

</mapper>
